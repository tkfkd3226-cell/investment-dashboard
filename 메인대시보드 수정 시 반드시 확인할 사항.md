첨부한 **최신 투자 대시보드 전체 ZIP**을 기준으로 메인 투자 성과 대시보드의 CSS 내부 구조를 정리해줘.

현재 프로젝트는 CSS, 메인 JavaScript, 투자 계산기 영역에 대한 대규모 리팩토링이 완료된 상태이며, **현재 구조를 새로운 기준선으로 취급한다.**

현재 주요 디렉토리 구조는 다음과 같다.

```text
investment-dashboard-main/
│
├─ index.html
│
├─ css/
│  └─ style.css                  ← 메인 투자 성과 대시보드 CSS
│
├─ js/                           ← 메인 투자 성과 대시보드 JavaScript
│  ├─ dashboard-core.js          ← 데이터 / 상태 / 계산 / 공통 helper
│  ├─ dashboard-charts.js        ← 차트
│  ├─ dashboard-ui.js            ← 일반 UI / Topbar / Navigation
│  ├─ dashboard-pension.js       ← 퇴직연금
│  └─ dashboard-app.js           ← 이벤트 연결 / render orchestration / boot
│
└─ add/                          ← 메인 외 부가 기능
   ├─ calc.html                  ← 투자 계산기 HTML
   │
   ├─ css/
   │  ├─ common.css              ← calc/report 공통 CSS
   │  └─ calc.css                ← 계산기 전용 CSS
   │
   ├─ js/
   │  └─ calc.js                 ← 계산기 전용 JavaScript
   │
   └─ report/
      └─ *.html
```

이번 작업의 **실제 수정 대상은 `css/style.css` 하나뿐**이다.

다른 파일은 CSS selector의 사용처와 DOM 관계를 확인하기 위해 필요한 범위에서 읽을 수 있지만 수정하지 않는다.

특히 다음은 이번 작업에서 수정하지 않는다.

```text
index.html
js/dashboard-core.js
js/dashboard-charts.js
js/dashboard-ui.js
js/dashboard-pension.js
js/dashboard-app.js

add/calc.html
add/css/common.css
add/css/calc.css
add/js/calc.js
add/report/
```

---

# 1. 작업 목적

메인 `css/style.css`는 이미 대규모 리팩토링을 통해 다음과 같은 구조 개선이 이루어진 상태다.

- 불필요한 `!important` 대폭 제거
- 중복 selector 제거
- 불필요한 breakpoint 제거
- 웹 / 태블릿 / 모바일 viewport 체계 정리
- 공통 component 정리
- CSS variable / design token 정리
- 누적 override 제거
- 동일 context에서 반복적으로 selector를 덮어쓰는 구조 정리

메인 JavaScript 역시 현재 역할별 5개 파일 구조로 리팩토링되어 있고, 계산기 또한 `calc.html / calc.css / calc.js`로 책임 분리가 완료되어 있다.

따라서 이번 작업은 기존 구조를 다시 대규모로 리팩토링하거나 코드량을 더 줄이는 것이 목적이 아니다.

목표는:

> **`css/style.css` 단일 파일 구조를 그대로 유지하면서, 기능과 역할별 섹션 배치를 명확하게 정리하여 앞으로 사람이나 GPT가 특정 기능의 CSS 위치를 빠르게 찾고 안전하게 수정할 수 있도록 만드는 것**

이다.

---

# 2. 현재 구조를 새로운 기준선으로 취급

과거의:

```text
단일 script.js
root calc.html
inline calc CSS/JS
```

구조를 기준으로 판단하지 않는다.

현재 최신 ZIP의 실제 구조만 기준으로 한다.

특히 CSS selector가 JavaScript에서 동적으로 생성되거나 class가 부여되는지 확인해야 한다면 현재의:

```text
js/dashboard-core.js
js/dashboard-charts.js
js/dashboard-ui.js
js/dashboard-pension.js
js/dashboard-app.js
```

를 필요한 범위에서 확인한다.

과거 `script.js`가 존재한다고 가정하지 않는다.

---

# 3. 메인 CSS는 단일 파일 유지

메인 대시보드 CSS는 앞으로도:

```text
css/style.css
```

**하나의 파일로 유지한다.**

다음처럼 역할별 파일로 분리하지 않는다.

```text
base.css
layout.css
components.css
charts.css
pension.css
responsive.css
...
```

특히 모든 반응형 CSS를 별도의 `responsive.css`로 분리하지 않는다.

현재 프로젝트에서는:

> **한 기능의 기본 스타일과 반응형 스타일을 하나의 CSS 파일 안에서 가까운 위치에 두는 것**

이 GPT 기반 유지보수와 cascade 추적에 더 적합하다.

---

# 4. 메인 CSS와 부가 페이지 CSS를 섞지 않는다

다음은 메인 대시보드 CSS가 아니다.

```text
add/css/common.css
add/css/calc.css
```

`add/css/common.css`는 calc/report 등 부가 페이지 공통 CSS이고,

```text
add/css/calc.css
```

는 계산기 전용 CSS다.

따라서 이번 작업에서:

- 메인 `style.css`와 통합하지 않는다.
- 공통화를 이유로 selector를 서로 이동시키지 않는다.
- calc와 메인 대시보드가 비슷한 UI를 가지고 있다는 이유만으로 CSS를 공유하게 만들지 않는다.

메인 대시보드와 `add/` 영역은 독립적으로 유지한다.

---

# 5. 기능별 기본 + 반응형 규칙을 같은 영역에서 관리

각 기능 영역 안에서 가능한 한 다음 흐름으로 관리한다.

```text
기본 공통 규칙
↓
Desktop · 웹
↓
Tablet · 태블릿
↓
Mobile · 모바일
↓
기능상 필요한 특수 예외
```

예:

```css
/* =========================================================
   Charts · 차트
   ========================================================= */

.chart-card {
  ...
}

.chart-control {
  ...
}

/* Desktop · 웹 */
@media (min-width:1101px) {
  ...
}

/* Tablet · 태블릿 */
@media (min-width:761px) and (max-width:1100px) {
  ...
}

/* Mobile · 모바일 */
@media (max-width:760px) {
  ...
}
```

반대로 다음처럼 같은 기능의 CSS가 파일 곳곳에 다시 흩어지지 않게 한다.

```text
파일 상단        → 기본 chart CSS
파일 중간        → tablet chart CSS
파일 하단        → mobile chart CSS
파일 마지막      → chart 최종 보정
```

핵심 원칙:

> **한 기능을 수정할 때 그 기능의 기본·웹·태블릿·모바일 CSS를 가능한 한 한 영역에서 확인할 수 있어야 한다.**

---

# 6. 기본 viewport 체계 유지

메인 대시보드 기본 viewport는 다음과 같다.

```text
Desktop · 웹      : 1101px 이상
Tablet · 태블릿   : 761px ~ 1100px
Mobile · 모바일   : 760px 이하
```

이번 작업에서 이 기준을 변경하지 않는다.

현재 기능상 필요한 예외 breakpoint가 존재한다면 그대로 유지한다.

이번 작업을 이유로 새로운 breakpoint를 추가하지 않는다.

목표:

```text
새 breakpoint: 0
```

---

# 7. CSS 영역 분류는 현재 JS 책임 구조도 참고

현재 메인 JavaScript는 역할별로 다음과 같이 분리되어 있다.

```text
dashboard-core.js
dashboard-charts.js
dashboard-ui.js
dashboard-pension.js
dashboard-app.js
```

CSS 내부 구조를 정리할 때 이 책임 경계를 참고할 수 있다.

예:

```text
dashboard-charts.js
→ Charts · 차트 CSS

dashboard-ui.js
→ Topbar / Navigation / 일반 UI CSS

dashboard-pension.js
→ Pension · 퇴직연금 CSS
```

다만 JavaScript 파일 구조를 CSS 구조와 **1:1로 억지로 맞추지는 않는다.**

예를 들어 `dashboard-app.js` 전용 CSS 같은 것은 만들 필요가 없다.

CSS는 CSS의 책임과 component 구조를 기준으로 판단한다.

---

# 8. 권장 대분류 섹션

현재 실제 `style.css`를 먼저 분석한 뒤 가장 자연스러운 영역 구성을 결정한다.

기본적으로 다음 정도의 큰 구조를 우선 검토한다.

```text
01. Variables / Base · 변수 / 기본 스타일
02. Global Layout / Shell · 전체 레이아웃 / 기본 구조
03. Topbar / Navigation · 상단바 / 내비게이션
04. Common Cards / Components · 공통 카드 / 컴포넌트
05. Tables / Forms · 테이블 / 입력 폼
06. Securities · 증권계좌
07. Pension · 퇴직연금
08. Charts · 차트
09. Modal / Tooltip / Overlay · 모달 / 툴팁 / 오버레이
10. Browser / Accessibility Exceptions · 브라우저 / 접근성 예외
```

실제 최신 CSS를 분석했을 때 다른 영역 구성이 더 자연스럽다면 섹션명이나 순서는 조정해도 된다.

단:

- 지나치게 많은 대분류를 만들지 않는다.
- CSS를 억지로 위 분류에 끼워 넣지 않는다.
- 하나의 기능을 여러 대분류에 불필요하게 분산시키지 않는다.

---

# 9. 대분류 섹션 주석은 영어 + 한글 병기

모든 주요 대분류 섹션은 영어 명칭과 한글 설명을 함께 사용한다.

예:

```css
/* =========================================================
   01. Variables / Base · 변수 / 기본 스타일
   ========================================================= */

/* =========================================================
   02. Global Layout / Shell · 전체 레이아웃 / 기본 구조
   ========================================================= */

/* =========================================================
   03. Topbar / Navigation · 상단바 / 내비게이션
   ========================================================= */

/* =========================================================
   04. Common Cards / Components · 공통 카드 / 컴포넌트
   ========================================================= */

/* =========================================================
   05. Tables / Forms · 테이블 / 입력 폼
   ========================================================= */

/* =========================================================
   06. Securities · 증권계좌
   ========================================================= */

/* =========================================================
   07. Pension · 퇴직연금
   ========================================================= */

/* =========================================================
   08. Charts · 차트
   ========================================================= */

/* =========================================================
   09. Modal / Tooltip / Overlay · 모달 / 툴팁 / 오버레이
   ========================================================= */

/* =========================================================
   10. Browser / Accessibility Exceptions · 브라우저 / 접근성 예외
   ========================================================= */
```

실제로 적용한 최종 섹션명을 기준으로 영어 + 한글을 병기한다.

---

# 10. 주요 하위 주석도 영어 + 한글 병기

탐색에 실질적으로 도움이 되는 주요 하위 기능에는 영어와 한글을 같이 사용한다.

예:

```css
/* KPI Cards · 핵심지표 카드 */

/* Mini Cards · 미니카드 */

/* Data Tables · 데이터 테이블 */

/* Mobile Cards · 모바일 카드 */

/* Chart Controls · 차트 조작 버튼 */

/* Date Controls · 날짜 선택 */

/* Asset Allocation · 자산배분 */

/* Pension Contribution · 퇴직연금 납입 */

/* Securities Performance · 증권계좌 성과 */

/* Expanded Chart · 차트 확대 */

/* Custom Tooltip · 커스텀 툴팁 */
```

단, 모든 selector마다 주석을 달지 않는다.

주석의 목적은:

> **사람이나 GPT가 원하는 기능 영역을 빠르게 찾게 하는 것**

이다.

---

# 11. 주석을 지나치게 세분화하지 않는다

다음처럼 개별 selector마다 설명을 붙이지 않는다.

```css
/* Button · 버튼 */
.button { ... }

/* Button Icon · 버튼 아이콘 */
.button-icon { ... }

/* Button Text · 버튼 텍스트 */
.button-text { ... }
```

주석은:

- 큰 섹션
- 의미 있는 하위 기능
- 이해하기 어려운 특수 예외

정도에만 사용한다.

---

# 12. 누적 패치형 주석 금지

다음과 같은 주석은 만들지 않는다.

```css
/* Fix */
/* Final */
/* Final Fix */
/* Mobile Fix */
/* Override */
/* New */
/* Temp */
/* Last Fix */
```

변경 이력 역시 CSS에 남기지 않는다.

```css
/* 1차 수정 */
/* 2026-08-14 수정 */
/* GPT 수정 */
```

CSS에는 현재 최종 구조와 존재 이유만 표현한다.

---

# 13. 특수 예외 주석 역시 영어 + 한글

기능상 필요한 브라우저 또는 viewport 예외는 이유가 드러나도록 짧게 표시한다.

예:

```css
/* iPhone Landscape · 아이폰 가로모드 예외 */

/* Safari Date Input · Safari 날짜 입력 보정 */

/* Reduced Motion · 모션 최소화 접근성 */

/* Print Layout · 인쇄 레이아웃 */
```

`Fix`, `Override` 같은 표현만 남기지 않는다.

---

# 14. Topbar / Navigation · 상단바 / 내비게이션

Topbar는 웹·태블릿·모바일에서 구조 차이가 큰 영역이므로 독립 섹션으로 관리한다.

가능하면 이 영역 안에서:

- 공통 topbar
- 날짜 선택
- 메뉴 버튼
- Desktop · 웹
- Tablet · 태블릿
- Mobile · 모바일
- landscape/browser 예외

를 함께 확인할 수 있게 한다.

현재 Topbar 동작 로직은 주로:

```text
js/dashboard-ui.js
js/dashboard-app.js
```

에서 관리되므로 selector 사용 여부가 불명확할 경우 이 파일들을 읽어서 확인한다.

단, JavaScript는 수정하지 않는다.

---

# 15. Common Cards / Components · 공통 카드 / 컴포넌트

두 개 이상의 기능 영역에서 실제 재사용되는 UI를 관리한다.

예:

- 공통 card
- mini-card
- button/control
- toggle
- badge
- mobile-data-card
- section title
- explainer
- positive / negative
- status
- 공통 form component

특정 기능 전용 selector를 억지로 공통 영역으로 올리지 않는다.

---

# 16. Tables / Forms · 테이블 / 입력 폼

여러 영역에서 재사용하는 다음 component를 필요에 따라 관리한다.

- table
- table cell
- input
- select
- label
- form field
- form action
- 모바일 table/card 전환

특정 Pension 또는 Securities 기능에만 사용하는 form/table은 해당 기능 영역에 남기는 것이 더 자연스러운지 판단한다.

---

# 17. Securities · 증권계좌

증권계좌 전용 UI는 가능한 한 하나의 큰 영역에서 관리한다.

예:

- 증권계좌 현황
- 보유 종목
- 종목별 성과
- 자산배분
- 증권 전용 card
- 증권 전용 table
- 증권 전용 responsive 규칙

동적 class나 DOM 생성 관계를 확인해야 할 경우 현재 JS 구조에서 관련 사용처를 검색한다.

---

# 18. Pension · 퇴직연금

퇴직연금 관련 CSS는 가능한 한 하나의 큰 기능 영역으로 관리한다.

예:

- 퇴직연금 현황
- 상품별 성과
- 납입
- 금액 조정
- 현금성 자산
- ETF 관련 UI
- Pension 전용 form
- Pension 전용 card/table
- Pension responsive 규칙

동적 DOM과 class는 현재:

```text
js/dashboard-pension.js
```

를 우선 확인한다.

필요하면 `dashboard-app.js`의 event 연결도 읽을 수 있지만 수정하지 않는다.

---

# 19. Charts · 차트

차트 관련 CSS는 가능한 한 하나의 큰 영역에서 관리한다.

예:

- chart container
- chart card 전용 규칙
- SVG
- axis
- bar
- line
- legend
- chart controls
- chart scroll
- chart expand
- animation
- responsive chart UI
- 차트 전용 tooltip

동적으로 생성되는 chart DOM/class를 확인할 경우:

```text
js/dashboard-charts.js
```

를 기준으로 확인한다.

일반적인 공통 card 자체까지 Charts 영역으로 이동하지는 않는다.

---

# 20. Modal / Tooltip / Overlay · 모달 / 툴팁 / 오버레이

다음 UI를 가능한 한 명확하게 관리한다.

- modal
- backdrop
- tooltip
- floating popup
- overlay
- menu overlay

특정 기능에 강하게 결합된 overlay/tooltip은 해당 기능 영역에 두는 것이 더 자연스러운지 판단한다.

---

# 21. Variables / Base · 변수 / 기본 스타일

파일 앞부분에는 실제 전역 기반을 둔다.

예:

- `:root`
- 전역 CSS variables
- design tokens
- reset
- `box-sizing`
- body
- 기본 typography
- 전역 color
- 진짜 공통 utility

특정 component에서만 쓰는 변수까지 무조건 전역으로 올리지 않는다.

---

# 22. CSS variable 위치 원칙

CSS custom property는 성격을 구분한다.

### Global Tokens · 전역 토큰

여러 기능에서 사용하는 변수.

### Component Variables · 컴포넌트 변수

특정 component에서만 사용하는 변수.

### Runtime Variables · 실행 중 변수

메인 JavaScript가 runtime에서 설정하는 custom property.

Runtime variable 여부가 불명확하면 현재 `js/*.js`에서 해당 변수명을 검색해서 확인한다.

확인 없이 사용하지 않는 변수라고 판단해 삭제하지 않는다.

---

# 23. `@keyframes` 위치

특정 component에서만 사용하는 animation은 해당 기능 영역 가까이에 둔다.

여러 영역에서 실제로 공유되는 animation만 공통 영역으로 둘 수 있다.

animation 이름과 동작 자체는 변경하지 않는다.

---

# 24. CSS 이동 전 cascade 분석 필수

이번 작업에서 가장 중요한 원칙이다.

현재 CSS는 source order에 따라 최종 결과가 달라질 수 있으므로 selector 이름만 보고 단순 이동하지 않는다.

rule 이동 전에 필요한 범위에서 확인한다.

- selector specificity
- source order dependency
- 동일 element에 적용되는 다른 selector
- 상태 selector
- pseudo-class / pseudo-element
- media query
- CSS variables
- animation / keyframes
- browser exception
- 기존 예외 처리
- JavaScript에서 동적으로 추가/제거되는 class

특히 현재 메인 JS가 5개 파일로 분리되어 있으므로 동적 class 사용처는 **현재 `js/*.js` 전체를 기준으로 필요한 범위에서 검색**한다.

---

# 25. JS는 참조용으로만 읽는다

CSS selector의 사용 여부나 동적 DOM 구조 확인을 위해 다음 파일을 읽는 것은 허용한다.

```text
js/dashboard-core.js
js/dashboard-charts.js
js/dashboard-ui.js
js/dashboard-pension.js
js/dashboard-app.js
```

그러나 이번 작업에서 이 파일들은 **절대 수정하지 않는다.**

CSS 구조 정리를 이유로:

- class 이름 변경
- data attribute 변경
- event 구조 변경
- JS selector 변경
- render 함수 변경

을 하지 않는다.

필요한 CSS 이동이 JS 변경까지 요구한다면 그 이동은 이번 작업에서 하지 않는다.

---

# 26. 위험한 CSS는 억지로 이동하지 않는다

특정 rule이 현재 위치에 있어야 cascade가 정상 유지된다면 섹션을 예쁘게 만들기 위해 이동하지 않는다.

우선순위는:

```text
기능 / UI 동일성
>
cascade 안전성
>
섹션 정렬
```

이다.

필요하면 현재 위치를 유지하고 그 이유를 결과 보고에 표시한다.

---

# 27. selector와 declaration은 원칙적으로 변경하지 않는다

이번 작업은 CSS 재설계가 아니라:

```text
CSS 내부 위치 / 섹션 / 주석 구조 정리
```

다.

따라서 특별한 이유가 없다면 변경하지 않는다.

- selector
- class 이름
- declaration
- CSS value
- specificity
- margin
- padding
- width/height
- grid
- flex
- font
- color
- animation
- transition
- CSS variable 값

사용자가 보는 UI는 작업 전후 동일해야 한다.

---

# 28. 새 `!important` 금지

이번 작업에서 새로운 `!important`가 생길 이유가 없다.

목표:

```text
새 !important: 0
```

CSS 위치 변경 때문에 `!important`가 필요해졌다면 rule 이동이나 cascade 설계가 잘못된 것으로 판단한다.

---

# 29. 새 breakpoint 금지

이번 작업에서 새로운 media breakpoint를 추가하지 않는다.

목표:

```text
새 breakpoint: 0
```

CSS 이동 후 특정 viewport가 깨진다고 새 media query를 만들어 보정하지 않는다.

---

# 30. 동일 selector override를 새로 만들지 않는다

다음 구조를 새로 만들지 않는다.

```css
.component {
  ...
}

/* 한참 아래 */

.component {
  ...
}
```

동일 media context에서도 같은 selector를 뒤에서 다시 덮는 구조를 새로 만들지 않는다.

---

# 31. 중복 발견 시 대규모 추가 리팩토링 금지

이번 과정에서 중복이나 개선 후보를 발견해도 이번 목적과 관계없는 리팩토링을 같이 수행하지 않는다.

완전히 동일하고 제거 영향이 없다는 것이 명백한 경우만 최소 정리를 검토한다.

그 외에는 개선 후보로만 보고한다.

---

# 32. Responsive CSS를 별도 영역으로 몰지 않는다

다음과 같은 구조를 만들지 않는다.

```text
Responsive
├─ Desktop 전체
├─ Tablet 전체
└─ Mobile 전체
```

대신 기능 중심으로 관리한다.

```text
Topbar
 ├─ 기본
 ├─ Tablet
 └─ Mobile

Charts
 ├─ 기본
 ├─ Tablet
 └─ Mobile

Pension
 ├─ 기본
 ├─ Tablet
 └─ Mobile
```

---

# 33. media query 숫자 감소 자체를 목표로 하지 않는다

같은:

```css
@media (max-width:760px)
```

가 여러 기능 영역에 있어도 component 근처에 두는 것이 유지보수에 적합하면 유지한다.

목표는 media query 수 감소가 아니라:

> **관련 CSS를 빠르게 찾고 안전하게 수정할 수 있는 구조**

다.

---

# 34. 이번 작업에서 하지 않는 것

이번에는 다음을 하지 않는다.

- CSS 파일 분리
- `responsive.css` 생성
- UI 디자인 변경
- 카드 열 수 변경
- spacing 조정
- 색상 변경
- font 변경
- breakpoint 변경
- selector rename
- DOM 변경
- JavaScript 변경
- 메인 JS 구조 변경
- `add/calc.html` 변경
- `add/css/common.css` 변경
- `add/css/calc.css` 변경
- `add/js/calc.js` 변경
- `add/report/` 변경
- 새로운 기능 추가
- 전체 CSS 재설계

---

# 35. Calc 리팩토링 결과를 건드리지 않는다

계산기는 이미 다음 구조로 독립 리팩토링이 완료되어 있다.

```text
add/calc.html
add/css/common.css
add/css/calc.css
add/js/calc.js
```

이번 메인 CSS 내부 구조 정리와 계산기는 별개의 작업이다.

메인 `style.css`와 계산기 `calc.css` 사이에서 selector를 이동하거나 공통화하지 않는다.

---

# 36. 작업 방식

현재 최신 `css/style.css`를 처음부터 직접 분석한 뒤 전체 섹션 구조를 확정하고 한 번에 정리해도 된다.

기능 개발 작업이 아니므로 불필요하게 여러 차수로 나누지 않는다.

다만 cascade 위험이 있는 영역은 억지로 이동하지 않는다.

---

# 37. 작업 속도 보호

이번 작업 때문에 프로젝트 전체를 장시간 QA하지 않는다.

검증은 `css/style.css` 내부 재배치와 직접 관련된 범위에 집중한다.

기본 확인:

- CSS 문법
- rule 누락
- rule 중복
- selector 변경 여부
- declaration 변경 여부
- media query 보존
- CSS variable 보존
- `@keyframes` 보존
- source order / cascade 위험
- 새 `!important`
- 새 breakpoint
- 예상 외 diff

JS 파일은 selector/DOM 관계 확인에 필요한 범위에서만 읽는다.

전체 메인 JS 기능 QA를 다시 수행하지 않는다.

전체 DOM/mock 또는 모든 viewport 브라우저 검증은 내가 별도로 요청한 경우에만 수행한다.

---

# 38. 가장 중요한 검증: CSS 내용 동일성

가능하면 작업 전후 CSS를 구조적으로 비교한다.

목표:

> **rule 위치와 주석은 달라질 수 있지만 실제 스타일 결과를 만드는 내용은 동일해야 한다.**

의도하지 않은 다음 변화가 없어야 한다.

- selector 변경
- selector 삭제
- selector 추가
- declaration 값 변경
- media query 조건 변경
- CSS variable 값 변경
- `@keyframes` 변경

---

# 39. CSS 줄 수 / 파일 크기 보고

작업 완료 후 다음을 보고한다.

- 수정 전 `css/style.css` 전체 줄 수
- 수정 후 전체 줄 수
- 증감 줄 수
- 수정 전 파일 크기
- 수정 후 파일 크기
- 증감 크기

영어+한글 섹션 주석 추가로 소폭 증가하는 것은 문제로 보지 않는다.

CSS 줄 수 감소 자체가 목표는 아니다.

---

# 40. 구조 보호 결과 보고

작업 후 반드시 다음을 짧게 보고한다.

```text
새 !important:
새 breakpoint:
selector 변경:
declaration 값 변경:
media query 조건 변경:
DOM 변경:
메인 JS 변경:
add 영역 변경:
예상 외 diff:
```

정상 목표:

```text
새 !important: 0
새 breakpoint: 0
selector 변경: 없음
declaration 값 변경: 없음
media query 조건 변경: 없음
DOM 변경: 없음
메인 JS 변경: 없음
add 영역 변경: 없음
예상 외 diff: 없음
```

---

# 41. 최종 CSS 목차 보고

정리 완료 후 실제 최종 `style.css`의 큰 구조를 목차 형태로 알려준다.

예:

```text
01. Variables / Base · 변수 / 기본 스타일
02. Global Layout / Shell · 전체 레이아웃 / 기본 구조
03. Topbar / Navigation · 상단바 / 내비게이션
04. Common Cards / Components · 공통 카드 / 컴포넌트
05. Tables / Forms · 테이블 / 입력 폼
06. Securities · 증권계좌
07. Pension · 퇴직연금
08. Charts · 차트
09. Modal / Tooltip / Overlay · 모달 / 툴팁 / 오버레이
10. Browser / Accessibility Exceptions · 브라우저 / 접근성 예외
```

실제 적용한 최종 구조를 기준으로 작성한다.

---

# 42. 결과물 전달

실제로 수정된 파일만 전달한다.

정상적으로는:

```text
css/style.css
```

하나만 변경되어야 한다.

ZIP으로 전달한다면 프로젝트 폴더 구조를 유지한다.

```text
patch/
└─ css/
   └─ style.css
```

다른 파일을 수정하지 않았다면 포함하지 않는다.

---

# 43. 최종 목표

최종 `css/style.css`는 하나의 파일로 유지하면서 다음 조건을 만족해야 한다.

- 원하는 기능의 CSS 위치를 빠르게 찾을 수 있음
- 큰 기능 영역이 명확함
- 영어와 한글로 동시에 검색하기 쉬움
- 해당 기능의 기본/태블릿/모바일 규칙을 가까운 위치에서 확인 가능
- cascade를 추적하기 쉬움
- 현재 5개 메인 JS 책임 구조와 CSS 기능 영역의 관계를 이해하기 쉬움
- 향후 GPT가 기능 수정 시 올바른 CSS/JS 책임 영역을 빠르게 찾을 수 있음
- patch CSS가 파일 하단에 다시 누적되기 어려움
- 기존 UI와 기능은 그대로 유지됨
- 완료된 메인 JS 및 calc 리팩토링 구조를 훼손하지 않음

가장 중요한 원칙은 다음이다.

> **메인 대시보드 CSS는 `css/style.css` 단일 파일 구조를 유지한다.**

> **역할별 CSS 파일 분리는 하지 않는다.**

> **각 기능 영역의 기본·웹·태블릿·모바일 규칙을 가능한 한 같은 영역 안에서 관리한다.**

> **주요 섹션과 의미 있는 하위 주석은 영어 + 한글을 함께 사용한다.**

> **현재 `js/` 5파일 구조와 `add/calc.html + add/css/calc.css + add/js/calc.js` 구조를 새로운 기준선으로 취급하고 훼손하지 않는다.**

> **이번 작업은 스타일이나 기능을 변경하는 리팩토링이 아니라, 이미 정리된 CSS를 앞으로 안전하게 관리하기 위한 내부 구조 정리다.**

첨부한 최신 ZIP을 직접 확인한 뒤 위 원칙에 맞게 **`css/style.css`만 정리하고 결과물을 만들어줘.**